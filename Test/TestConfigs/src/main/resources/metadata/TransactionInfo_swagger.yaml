openapi: 3.0.1
info:
  title: TransactionInfo
  description: API Definition of TransactionInfo
  version: 1.0.0
servers:
- url: http://localhost:8290/transaction
paths:
  /{account_id}:
    get:
      parameters:
      - name: account_id
        in: path
        required: true
        style: simple
        explode: false
        schema:
          type: string
      responses:
        default:
          description: Default response
